# Implementer browser observations

12 September 2026, after the final stamp287 source export, lab build and
Flight Studio build. Local server served the worktree's `docs` directory at
`http://127.0.0.1:8791/lab/premium-pilots/` in the Codex in-app browser.
This is separate from the independent auditor's native production-renderer
captures and is not an independent browser review.

- Reloaded the final build and observed four complete 9/9 motion banks.
  Cyber, Percy, Envoy and Patriot reported matching frame and velocity
  labels throughout the sampled controls.
- Inspected Envoy's crossing and the final Patriot ascent7 and descent6
  paintings enlarged; no added helmet overlay, detached costume fragment,
  cut gap or visible loading failure was observed.
- Reloaded after the last two anticipation repairs and inspected Patriot
  ascent2 and ascent3 enlarged. The second downward step remains visible;
  frame/velocity labels match the other three rows.
- Selected 52px game size and quarter speed, disabled repeated taps,
  pressed Reset and Dive, and observed the four rows progress together.
  Percy and Envoy's visible signature wakes remained attached to their
  painted boot region; all three boot emitters are checked mechanically.
- Restored enlarged/normal speed with repeated taps and wakes enabled.
  The final console error/warning query returned no entries.

Earlier responsive inspection at390px showed a single-column comparison;
the temporary viewport override was reset. These observations sample the
local comparison page, not an installed-device release or live deployment.
All lab state is local and temporary; no player save was modified.

The retained filmstrips and source/output/runtime hashes are in the
independent review beside this receipt. Browser snapshots do not prove
every possible input or display-refresh combination.

Face registration uses the first cell as the reference, head centre (240,144), radius 43 in source pixels. Fit translation and uniform scale over source rectangle x190..284/y100..184 at 2px intervals, using a soft-L1 RGB residual (scale .03). Each target samples the complete cell bilinearly. Scale stays within .94–1.06. This excludes the tail silhouette. `tail-heads.json` records all sixteen results; `registration.json` records the runtime mapping.
